#include <vector>

int factorial(long);
int combination(int, int);

std::vector<std::vector<int>> generate( int numRows ) {
    if(numRows==1) return {{1}};
    std::vector<std::vector<int>> third = {{1, 1}};
} 

